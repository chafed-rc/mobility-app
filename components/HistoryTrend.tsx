// HistoryTrend.tsx

import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

interface TrendProps {
  title: string;
  value: number;
  trend: string;
}

const HistoryTrend: React.FC<TrendProps> = ({ title, value, trend }) => {
  return (
      <>
        
      </>
  );
};



export default HistoryTrend;
